import { Directive, ElementRef } from '@angular/core';

@Directive({
  selector: '[side-container]',
})
export class SideContainerDirective {
  constructor(el: ElementRef) {
    el.nativeElement.className += "container-jump-out-side";
  }
}
