import { Directive, ElementRef } from '@angular/core';

@Directive({
  selector: '[sidenav-item]',
})
export class SidenavItemDirective {
  constructor(el: ElementRef) {
    el.nativeElement.className += "sidenav-item"
  }
}
