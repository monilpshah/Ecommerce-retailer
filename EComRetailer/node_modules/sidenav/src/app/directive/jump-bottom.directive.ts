import { Directive } from '@angular/core';
import { ElementRef } from '@angular/core';

@Directive({
  selector: '[jump-bottom]',
})
export class JumpBottomDirective {
  constructor(el: ElementRef) {
    el.nativeElement.className += "jump-to-bottom";
  }
}
